# 核心配置
remote_theme: pages-themes/architect@v0.2.0  # 指定主题和版本
plugins:
  - jekyll-remote-theme  # 必须启用
  - jekyll-seo-tag       # 可选SEO插件
  - jekyll-feed          # 可选RSS插件

# 基础信息（自定义以下内容）
title: "你的网站标题"
description: "网站描述"
url: "https://你的用户名.github.io"  # 替换为你的GitHub Pages地址
